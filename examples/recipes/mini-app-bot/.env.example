TOKEN=
# publicly-reachable https origin the telegram client will load the mini app from
# during dev, expose localhost with ngrok / cloudflared / localtunnel
# example: https://abcd-1234.ngrok-free.app
PUBLIC_URL=
# local port fastify binds to
PORT=3000
