{
  "name": "puregram-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.0.0"
  },
  "devDependencies": {
    "@types/common-tags": "^1.8.1",
    "@types/debug": "^4.1.7",
    "@types/jest": "^27.0.2",
    "cheerio": "^1.0.0-rc.10",
    "common-tags": "^1.8.0",
    "jest": "^27.2.4",
    "ts-jest": "^27.0.5",
    "ts-node": "github:TypeStrong/ts-node#master",
    "typedoc": "^0.22.5",
    "typescript": "^4.4.3",
    "undici": "^5.0.0"
  },
  "scripts": {
    "prepare": "yarn run test",
    "test": "jest --config jest.config.js --no-cache",
    "generate": "yarn generate-constants && yarn generate-updates",
    "generate-constants": "ts-node scripts/generate-constants/",
    "generate-interfaces": "ts-node scripts/generate-interfaces/",
    "generate-updates": "ts-node scripts/generate-updates/"
  }
}