// re-export — canonical home is `@puregram/api/custom-update`
// (so `Filter<T>` can reference `AnyUpdate` without a reverse dep)
export { CustomUpdate, CustomUpdateRegistry } from '@puregram/api'
export type { AnyUpdate } from '@puregram/api'
